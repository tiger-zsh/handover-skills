# {{项目名}} —— 新人入门

> 这份文档面向第一天到第一周内接手本项目的新人。目标是让你能跑起项目、跑通测试、找到该看的入口、知道第一个能动手的低风险任务。
>
> 每条信息后面都有**来源标记**：
> - `[代码确认]` —— 从代码 / 配置文件直接读出
> - `[用户确认]` —— 项目熟悉者已确认
> - `[待确认]` —— 推断，需要核实再行动
>
> **凡是带 `[待确认]` 的内容，动手前请先和项目 owner 核对。**

---

## 1. 这是什么项目

一句话说明项目目标 / 业务定位。

- **技术栈**：{{语言 + 主要框架}}  `[代码确认]`
- **运行形态**：{{web 服务 / CLI / library / 多服务}}  `[待确认]`
- **主要依赖外部服务**：{{数据库 / 队列 / 第三方 API}}  `[待确认]`

---

## 2. 环境准备

### 必需

```bash
# 例：
# Node 20+ / Python 3.11+ / Go 1.22+
```
`[代码确认 / 待确认]`

### 推荐

- 编辑器：{{VSCode / JetBrains 等}}  `[待确认]`
- 推荐插件：{{ESLint / ruff / gopls 等}}  `[待确认]`

### 系统依赖

- {{Docker / PostgreSQL / Redis ...}}  `[待确认]`

---

## 3. 本地启动

### 步骤一：克隆并安装依赖

```bash
git clone {{repo-url}}
cd {{repo-name}}
{{npm install | uv sync | go mod download | ...}}
```
`[代码确认]`

### 步骤二：配置环境变量

```bash
cp .env.example .env
# 编辑 .env，填入：
# - {{KEY_1}}: {{含义和获取方式}}  [待确认]
# - {{KEY_2}}: {{含义}}  [待确认]
```
`[代码确认 + 待确认]`

### 步骤三：启动

```bash
{{npm run dev | make run | docker compose up | ...}}
```
`[代码确认]`

### 启动成功的标志

- {{看到某条日志 / 访问 localhost:3000 看到首页 / ...}}  `[待确认]`

---

## 4. 跑测试

```bash
{{npm test | pytest | go test ./... | ...}}
```
`[代码确认]`

- **单测在哪**：`{{tests/ | __tests__/ | *_test.go}}`  `[代码确认]`
- **集成测试如何跑**：`{{...}}`  `[待确认]`
- **CI 在哪里看**：`{{GitHub Actions / GitLab CI / ...}}`  `[待确认]`

---

## 5. 核心入口（先看这 3-5 个）

按"新人理解项目应该按什么顺序看"排列：

1. **`{{path/to/entry}}`** —— {{这是什么、为什么先看}}  `[待确认]`
2. **`{{path/to/router-or-main}}`** —— {{...}}  `[待确认]`
3. **`{{path/to/domain-model}}`** —— {{...}}  `[待确认]`
4. **`{{path/to/config}}`** —— {{...}}  `[待确认]`
5. **`{{path/to/...}}`** —— {{...}}  `[待确认]`

> 这里只列入口，不展开模块边界 / 数据流。完整架构请看 `docs/architecture.md`（如果已有）。

---

## 6. 常见开发任务

| 想做的事 | 从哪开始 | 备注 |
|---|---|---|
| 加一个 API 接口 | `{{...}}` | `[待确认]` |
| 加一个数据库字段 | `{{...}}` | `[待确认]` |
| 改前端页面 | `{{...}}` | `[待确认]` |
| 加单测 | `{{...}}` | `[待确认]` |

---

## 7. 第一个练手任务

**建议**：{{具体描述：低风险、能闭环、不涉及生产数据、能让新人摸到主流程的小任务}}

- 涉及文件：`{{...}}`  `[待确认]`
- 估时：{{半天 / 一天}}  `[待确认]`
- 验收标准：{{...}}  `[待确认]`

> 选这个任务的原因：{{为什么它适合作为第一个任务——通常是"动到主流程但不动核心逻辑"}}

---

## 8. 关键联系人 / 模块 owner

| 模块 / 领域 | 找谁 | 备注 |
|---|---|---|
| {{后端 / 前端 / 数据 / 部署}} | {{...}} | `[待确认]` |

> 如果项目是单人维护，这一节可以删掉或写"暂无"。

---

## 9. 接下来该看什么

按顺序：

1. **`docs/architecture.md`** —— 系统整体结构、模块边界、数据流（如已生成）
2. **`docs/runbook.md`** —— 部署、回滚、备份、告警（如已生成）
3. **`docs/sop/INDEX.md`** —— 历史故障剧本（如已生成）

如果以上文档还没有：用对应的 skill 生成（`/runbook`、`/architecture`、或遇到具体错误时让 `sop` 自动触发）。

---

## 10. 已知坑

> 列那些"看代码看不出来，但踩过的人都知道"的坑。新坑随手追加。

- {{...}}  `[待确认]`

---

<!--
维护备注（渲染视图中不会展示；raw Markdown 中可见）：

- 凡是 [待确认] 项，应该在和项目 owner 一次对话中清掉。
- 文档总长度建议控制在 200 行以内。超过说明在抢 architecture-map / runbook 的职责，需要拆分。
- 更新时不要全量重写，只改差异；带 [用户确认] 的段落要保留。
-->
